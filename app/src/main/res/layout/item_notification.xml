<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="3dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Первая строка: Заголовок, дата, кнопка удаления -->
        <TextView
            android:id="@+id/text_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceSubtitle2"
            android:textColor="?android:attr/textColorPrimary"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/text_date"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Запрос на доступ к дереву" />

        <TextView
            android:id="@+id/text_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceCaption"
            android:textColor="?android:attr/textColorTertiary"
            android:textSize="11sp"
            android:layout_marginEnd="4dp"
            app:layout_constraintEnd_toStartOf="@id/btn_delete"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/text_title"
            tools:text="13.06.2025" />
            
        <ImageView
            android:id="@+id/btn_delete"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:contentDescription="Удалить уведомление"
            android:src="@android:drawable/ic_menu_delete"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/text_title"
            app:tint="@android:color/darker_gray" />

        <!-- Вторая строка: Бейдж типа и статус -->
        <TextView
            android:id="@+id/text_type_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:background="@drawable/badge_background"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:textAppearance="?attr/textAppearanceCaption"
            android:textColor="@android:color/white"
            android:textSize="9sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/text_title"
            tools:text="ЗАПРОС НА ДОСТУП" />

        <TextView
            android:id="@+id/text_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textStyle="italic"
            android:textAppearance="?attr/textAppearanceCaption"
            android:textSize="11sp"
            android:textColor="?android:attr/textColorTertiary"
            app:layout_constraintStart_toEndOf="@id/text_type_badge"
            app:layout_constraintTop_toTopOf="@id/text_type_badge"
            app:layout_constraintBottom_toBottomOf="@id/text_type_badge"
            tools:text="Ожидает ответа" />

        <!-- Третья строка: Информация о пользователе и мемориале (в одной строке) -->
        <TextView
            android:id="@+id/text_user_info"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textAppearance="?attr/textAppearanceCaption"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="?android:attr/textColorSecondary"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/text_type_badge"
            tools:text="От: Тестова Подписка Подписковна" />

        <TextView
            android:id="@+id/text_memorial_info"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textAppearance="?attr/textAppearanceCaption"
            android:textSize="12sp"
            android:textColor="?android:attr/textColorSecondary"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/text_user_info"
            tools:text="Мемориал: Семья Ивановых" />

        <!-- Четвертая строка: Сообщение (сокращенное) -->
        <TextView
            android:id="@+id/text_message"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textAppearance="?attr/textAppearanceBody2"
            android:textSize="13sp"
            android:textColor="?android:attr/textColorSecondary"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/text_memorial_info"
            tools:text="Пользователь запрашивает доступ к вашему генеалогическому дереву для редактирования" />

        <!-- Пятая строка: Кнопки действий (компактные) -->
        <LinearLayout
            android:id="@+id/action_buttons"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/text_message">

            <Button
                android:id="@+id/btn_accept"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:layout_marginEnd="6dp"
                android:text="Принять"
                android:textSize="11sp"
                android:paddingHorizontal="12dp"
                android:paddingVertical="0dp"
                android:minWidth="0dp"
                android:minHeight="0dp"
                android:visibility="gone"
                style="@style/Widget.Material3.Button.TextButton"
                tools:visibility="visible" />

            <Button
                android:id="@+id/btn_reject"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="Отклонить"
                android:textSize="11sp"
                android:paddingHorizontal="12dp"
                android:paddingVertical="0dp"
                android:minWidth="0dp"
                android:minHeight="0dp"
                android:visibility="gone"
                style="@style/Widget.Material3.Button.TextButton"
                tools:visibility="visible" />
        </LinearLayout>

        <!-- Скрытый элемент для индикатора непрочитанности (можно использовать позже) -->
        <View
            android:id="@+id/view_unread"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:background="@color/purple_500"
            android:visibility="gone"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView> 